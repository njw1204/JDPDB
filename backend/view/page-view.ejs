<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1" name="viewport">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <link href="/css/page.css" rel="stylesheet">
    <link href="/css/main.css" rel="stylesheet">
    <link href="/css/menu.css" rel="stylesheet">
    <link href="/css/lightslider.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.plyr.io/3.5.6/plyr.css" />

    <script src="/js/lightslider.min.js"></script>
    <script defer src="/fontawesome/js/all.js"></script>

    <script>
        $(document).ready(function() {
            $(".light-slider").lightSlider({
            });
        });
    </script>

    <!-- Change Title and Description !-->
    <title>Animal Home</title>
</head>

<body>
<% include ./header %>

<!-- Page Content -->
<div class="container">
    <div class="container bootstrap-snippet header-container mb-5">
        <div style="background-image: url( '/media/fishnets-and-hearts.png' ); background-repeat: repeat;">
            <div class="container pt-5 pb-3 mt-5">
                <div class="d-inline-block ml-5">
                    <% if (profile_picture === null) { %>
                        <img class="sh-1 d-inline-block" alt="" src="/media/no-image.jpg" style="width: 300px; height: 200px; vertical-align: baseline;">
                    <% } else { %>
                        <img class="sh-1 d-inline-block" alt="" src="<%= profile_picture %>" style="width: 300px; height: 200px; vertical-align: baseline;">
                    <% } %>
                    <div class="d-inline-block" style="margin-left: 40px;">
                        <% if (category === "CAT") { %>
                            <h3 class="font-weight-bold mb-4 page-profile-title" id="pageID"><i class="fas fa-cat fa-fw mr-2"></i><%= animal_name %></h3>
                        <% } else if (category == "DOG") { %>
                            <h3 class="font-weight-bold mb-4 page-profile-title" id="pageID"><i class="fas fa-dog fa-fw mr-2"></i><%= animal_name %></h3>
                        <% } else if (category == "FISH") { %>
                            <h3 class="font-weight-bold mb-4 page-profile-title" id="pageID"><i class="fas fa-fish fa-fw mr-2"></i><%= animal_name %></h3>
                        <% } else if (category == "BIRD") { %>
                            <h3 class="font-weight-bold mb-4 page-profile-title" id="pageID"><i class="fas fa-dove fa-fw mr-2"></i><%= animal_name %></h3>
                        <% } else { %>
                            <h3 class="font-weight-bold mb-4 page-profile-title" id="pageID"><i class="fas fa-paw fa-fw mr-2"></i><%= animal_name %></h3>
                        <% } %>
                        <div class="text-muted mb-4" name="pageDescription" style="max-width: 240px; word-break: break-all;">
                            <%= description %>
                        </div>
                        <div class="text-black mb-4">
                            <a class="text-black">Owner: </a>
                            <span class="text-muted" id="pageEmail"><%= nickname %></span>
                            <br>
                            <a class="text-black">Category: </a>
                            <span class="text-muted" id="pageEmail"><%= category %></span>
                            <br><br>
                            <a class="d-inline-block text-dark">
                                <strong id="pageSubscribing"><%= subscribe_count %></strong>
                                <span class="text-muted">Subscribers</span>
                            </a>
                        </div>
                    </div>
                </div>
                <div class="mr-4" style="float: right; text-align: right;">
                    <% if (session.user && !isAdmin && typeof subscribe !== "undefined" && subscribe) { %>
                        <p class="text-black font-weight-bold">Now Subscribing!</p>
                    <% } else if (session.user && isAdmin) { %>
                        <p class="text-black font-weight-bold">I'm Owner!</p>
                    <% } %>
                    <% if (session.user && !isAdmin) { %>
                        <% if (typeof subscribe === "undefined" || !subscribe) { %>
                            <p><a class="btn btn-lg btn-primary" href="/page/subscribe/<%= id %>">Subscribe</a></p>
                        <% } else { %>
                            <p><a class="btn btn-lg btn-danger" href="/page/unsubscribe/<%= id %>">Unsubscribe</a></p>
                        <% } %>
                    <% } %>
                    <% if (session.user && !isAdmin) { %>
                        <p><a class="btn btn-lg btn-success" href="/page/support/<%= id %>">Support Animal</a></p>
                    <% } %>
                </div>
                <div style="clear: both;"></div>
            </div>
            <hr class="m-0">

        </div>
    </div>

    <div class="row">
        <!-- Post Content Column -->
        <div class="col-md-8 gedf-main" style="padding-left: 29px;">
            <% if (posts.length < 1) { %>
                <div class="card gedf-card" style="margin-bottom: 40px;">
                    <div class="card-body">
                        <h4 class="p-3">There are no posts &nbsp;:(</h4>
                    </div>
                </div>
            <% } %>
            <% for (let post of posts) { %>
                <% if (post.min_class_level > 0 && (!session.user || !isAdmin && my_class_level < post.min_class_level)) { %>
                    <% continue; %>
                <% } %>
            <!--- \\\\\\\Post-->
                <div class="card gedf-card" style="margin-bottom: 40px;">
                    <div class="card-body">
                        <div class="text-muted h7 mb-1"><%= post.created_time %></div>
                        <a class="card-link d-inline-block" href="#" onclick="return false;">
                            <% if (post.min_class_level < 1) { %>
                                <h5 class="card-title d-inline-block"><%= post.title %> (Visible to everyone)</h5>
                            <% } else { %>
                                <h5 class="card-title d-inline-block"><%= post.title %> (Visible to Lv.<%= post.min_class_level %>)</h5>
                            <% } %>
                        </a>
                        <% if (session.user && isAdmin) { %>
                            <form class="d-inline" method="POST" action="/page/unpost/<%= id %>?next=/page/<%= id %>" style="display: inline-block;">
                                <input type="hidden" name="post_id" value="<%= post.id %>"></input>
                                <button class="btn btn-link text-danger" type="submit" style="vertical-align: baseline;"><i class="fas fa-times"></i></button>
                            </form>
                        <% } %>

                        <% if (post.files && post.files.length > 0) { %>
                            <div id="carousel-<%= post.id %>" class="carousel slide" data-ride="carousel" data-interval="0" style="width: 400px; height: 225px; cursor: pointer;">
                                <div class="carousel-inner w-100 h-100">
                                    <% let idx = 0; %>
                                    <% for (let file of post.files) { %>
                                        <% if (file.url.indexOf("youtube.com") != -1 || file.url.indexOf("youtu.be") != -1) { %>
                                            <div class="carousel-item w-100 h-100">
                                                <div class="plyr__video-embed player">
                                                    <iframe
                                                        src="<%= file.url %>"
                                                        allowfullscreen
                                                        allowtransparency
                                                        allow="autoplay"
                                                    ></iframe>
                                                </div>
                                            </div>
                                        <% } else { %>
                                            <% if (idx === 0) { %>
                                                <div class="carousel-item w-100 h-100 active" onclick="location.href='<%= file.url %>';">
                                                    <div class="w-100 h-100" style="background-position: center center; background-color: rgb(10,10,10); background-image: url( '<%= file.url %>' ); background-size: contain; background-repeat: no-repeat;"></div>
                                                </div>
                                                <% idx = 1; %>
                                            <% } else { %>
                                                <div class="carousel-item w-100 h-100" onclick="location.href='<%= file.url %>';">
                                                    <div class="w-100 h-100" style="background-position: center center; background-color: rgb(10,10,10); background-image: url( '<%= file.url %>' ); background-size: contain; background-repeat: no-repeat;"></div>
                                                </div>
                                            <% } %>
                                        <% } %>
                                    <% } %>
                                </div>
                                <a class="carousel-control-prev carousel-control" href="#carousel-<%= post.id %>" role="button" data-slide="prev">
                                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                    <span class="sr-only">Previous</span>
                                </a>
                                <a class="carousel-control-next carousel-control" href="#carousel-<%= post.id %>" role="button" data-slide="next">
                                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                    <span class="sr-only">Next</span>
                                </a>
                            </div>
                        <% } %>

                        <p id="post-<%= post.id %>" class="card-text py-3">
                            <%= post.content %>
                        </p>
                    </div>
                    <!-- Comments Form -->
                    <div class="card mb-4">
                        <h5 class="card-header">Leave a Comment:</h5>
                        <div class="card-body">
                            <% if (session.user) { %>
                                <form method="POST" action="/page/comment?next=/page/<%= id %>#post-<%= post.id %>">
                                    <div class="form-group">
                                        <input type="hidden" name="post_id" value="<%= post.id %>"></input>
                                        <textarea name="content" class="form-control" rows="3" maxlength="500" required></textarea>
                                    </div>
                                    <button type="submit" class="btn btn-primary">Submit</button>
                                </form>
                            <% } else { %>
                                You must be logged in to leave a comment.
                            <% } %>
                        </div>
                    </div>

                    <% for (let comment of post.comments) { %>
                        <% let hash = crypto.createHash("md5").update(comment.nickname).digest("hex"); %>
                        <!-- Single Comment -->
                        <div class="text-muted h7 ml-4 mb-2"><%= comment.created_time %></div>
                        <div class="media mb-4 ml-4 mr-4">
                            <img class="d-flex mr-3 rounded-circle" width="50" height="50"
                                src="https://www.gravatar.com/avatar/<%- hash %>?f=y&d=retro&s=50" alt="">
                            <div class="media-body">
                                <h5 class="d-inline-block mt-0"><%= comment.nickname %></h5>
                                <% if (session.user && comment.user_id === session.user.id) { %>
                                    <form class="d-inline" method="POST" action="/page/uncomment?next=/page/<%= id %>#post-<%= post.id %>" style="display: inline-block;">
                                        <input type="hidden" name="id" value="<%= comment.id %>"></input>
                                        <button class="btn btn-link text-danger" type="submit" style="vertical-align: bottom;"><i class="fas fa-times"></i></button>
                                    </form>
                                <% } %>
                                <p style="font-size: 0.9em; white-space: pre-wrap;"><%= comment.content %></p>
                            </div>
                        </div>
                    <% } %>
                </div>
            <% } %>
            <!-- Post /////-->
        </div>
        <!-- Donation Class List Column -->
        <div class="col-md-4" style="padding-right: 29px;">
            <div class="toDonate card gedf-card">
                <div class="card-body">
                    <h4 class="card-title text-middle"><i class="fas fa-donate mr-2"></i>Supporter Level</h4>
                    <h6 class="card-subtitle h7 mb-2 text-muted">We need your help!</h6>
                    <h6 class="card-subtitle h7 mb-4 text-muted">Support us and get rewards.</h6>

                    <p class="card-text mb-0" style="font-size: 16px;"> Your Level:
                        <% if (session.user && isAdmin) { %>
                            <strong id="yourRank">Owner (Lv.MAX)</strong>
                        <% } else if (my_class_level) { %>
                            <strong id="yourRank"><%= donate_classes[my_class_level - 1].class_name %> (Lv.<%= my_class_level %>)</strong>
                        <% } else { %>
                            <strong id="yourRank">Guest (Lv.0)</strong>
                        <% } %>
                    </p>
                    <p class="card-text mb-3" style="font-size: 16px;"> My Support:
                        <strong id="yourRank"><%= commaNumber(total_donate_from_me || 0) %>pts</strong>
                    </p>

                    <hr>

                    <h5>Lv.0. Guest</h5>
                    <ul>
                        <li>View public diarys</li>
                    </ul>
                    <% if (donate_classes.length > 0) { %>
                        <% let rewards = []; %>
                        <% for (let cls of donate_classes) { %>
                            <hr>
                            <% rewards.push(cls.reward_description); %>

                            <h5>Lv.<%= cls.class_level %>. <%= cls.class_name %> (<%= commaNumber(cls.cost) %>pts ~)</h5>
                            <ul>
                            <li>View Lv.<%= cls.class_level %> diarys</li>
                            <% for (let reward of rewards) { %>
                                <li><%= reward %></li>
                            <% } %>
                            </ul>
                        <% } %>
                    <% } %>
                </div>
            </div>
        </div>

    </div>
    <!-- /.row -->

</div>
<!-- /.container -->

<script src="https://cdn.plyr.io/3.5.6/plyr.js"></script>
<script>var player = new Plyr('.player');</script>
</body>
</html>
